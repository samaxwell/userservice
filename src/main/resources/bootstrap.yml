spring:
  application:
    name: 'userservice'

  profiles:
    active: 'dev'

  cloud:
    config:
#       discovery:
#          enabled: true
#      label: 'develop'
      uri: 'http://localhost:8888'
#      fail-fast: true